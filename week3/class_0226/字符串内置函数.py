# -*- coding: utf-8 -*-
# @Time    : 2020/5/13 14:13
# @Author  : zc
# @Email   : zc_9211@163.com
# @File    : 字符串内置函数.py
# @Software: PyCha

s = 'pytHonyy'
# res = s.replace('y', '@', 2)  # 替换 第一个参数是目标字符串 第二个是替换的字符串  第三个是替换的次数（默认是全部）
# res=s.split('y',1)#切割，返回的是列表 第一个参数是根据指定的字符进行切割 第二个参数 切割次数，默认是全部切割(-1)
# res=s.strip('y')#去除 默认去除头和尾的空格，也可以去掉你指定的（头和尾）字符
# res=s.swapcase()#大小写互换

# print(res)
